---
# Инвентарь боевого стенда. Один хост — сервис однопользовательский,
# кластер заданием не требуется.
#
# Подставьте адрес своего VPS, SSH-пользователя и путь к ключу.
all:
  children:
    doc3:
      hosts:
        doc3-vps:
          ansible_host: 103.76.52.3
          ansible_user: dimok
          ansible_ssh_private_key_file: /home/DIMOK/.ssh/yandex_ai_document
