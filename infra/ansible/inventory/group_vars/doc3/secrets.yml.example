---
# Скопируйте в secrets.yml и подставьте реальные значения.
# secrets.yml в .gitignore и в репозиторий попадать не должен.
#
# Сгенерировать пароль:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
postgres_password: replace-me
