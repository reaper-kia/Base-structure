fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1

# Единственная внешняя зависимость инференса: HTTP-вызов Ollama.
# Модель живёт в отдельном контейнере, локальных весов сервису не нужно.
httpx==0.28.1

pytest==9.1.0
ruff==0.8.4
